# cp

Utilitário `cp` para o ecossistema JeffNix.

Este é um utilitário escrito em Rust para o sistema operacional **JeffNix**.

## Uso

```bash
./cp [opções]
```

## Compilação

Para compilar este utilitário individualmente:

```bash
cargo build --release
```
